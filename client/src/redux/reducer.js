const initState = {
  darkMode: false,
  connect: {},
  notification: {},
  game: {},
  currentView: "home",
};

const reducer = (state = initState, action) => {
  return state;
};

export default reducer;
